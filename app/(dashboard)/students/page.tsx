import Link from "next/link";import { db } from "@/lib/db";import { Student,FeeRecord } from "@/lib/models";import { withTenant } from "@/lib/withTenant";
export default async function Students({searchParams}:{searchParams:{q?:string}}){await db();const q=searchParams.q||"",students=await Student.find(await withTenant({status:"active",...(q?{name:{$regex:q,$options:"i"}}:{})})).sort({name:1}).lean(),now=new Date(),dueIds=new Set((await FeeRecord.find(await withTenant({month:now.getMonth()+1,year:now.getFullYear(),status:{$ne:"paid"}})).select("studentId").lean()).map((f:any)=>String(f.studentId)));return <><div className="page-head"><h1>Students List</h1><span>{students.length}</span></div><form className="search"><span>⌕</span><input aria-label="Search student name" name="q" defaultValue={q} placeholder="Search student name..."/></form><section className="student-list">{students.map((s:any)=><Link className="student-card" key={String(s._id)} href={`/students/${s._id}`}><span className="avatar">{s.name.charAt(0).toUpperCase()}</span><div><b>{s.name}</b><small>{s.class} · ₹{s.monthlyFee.toLocaleString("en-IN")}/month</small></div><em>{dueIds.has(String(s._id))?"DUE SOON":"PAID"}</em><i>›</i></Link>)}</section><Link className="fab" href="/students/add" aria-label="Add student">＋</Link></>}
