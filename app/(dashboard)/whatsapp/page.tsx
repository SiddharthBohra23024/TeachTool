import { CheckCircle2,MessageSquare } from "lucide-react";import { PageHeader } from "@/app/components";import { WhatsAppConnect } from "@/app/whatsapp-connect";import { db } from "@/lib/db";import { Tenant } from "@/lib/models";import { session } from "@/lib/auth";
export default async function WhatsApp(){await db();const auth=(await session())!,tenant=await Tenant.findById(auth.tenantId),connected=!!tenant.whatsapp?.phoneNumberId;return <><PageHeader title="WhatsApp Business" back="/profile"/><div className="space-y-5 p-4"><section className="rounded-3xl border border-slate-100 bg-white p-5 text-center shadow-sm"><div className="mx-auto mb-3 flex h-16 w-16 items-center justify-center rounded-full bg-emerald-50 text-emerald-600"><MessageSquare className="h-8 w-8"/></div><h2 className="font-bold text-slate-800">Connect Your WhatsApp</h2><p className="mx-auto mt-1 max-w-sm text-xs leading-5 text-slate-500">Use your WhatsApp Business number for fee reminders, payment receipts, and broadcasts.</p></section><WhatsAppConnect connected={connected} phone={tenant.whatsapp?.displayPhoneNumber}/><section className="rounded-3xl border border-slate-100 bg-white p-5 shadow-sm"><h3 className="mb-3 text-xs font-bold uppercase tracking-wider text-slate-500">What this enables</h3><div className="space-y-3 text-sm text-slate-600">{["Automatic fee reminders","Payment receipts to parents","Class-wide broadcast messages"].map(item=><div key={item} className="flex items-center gap-2"><CheckCircle2 className="h-4 w-4 text-emerald-500"/>{item}</div>)}</div></section></div></>}
