import { CheckCircle2,UserPlus } from "lucide-react";import { notFound } from "next/navigation";import { db } from "@/lib/db";import { Tenant } from "@/lib/models";
export const dynamic="force-dynamic";
const input="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm focus:border-orange-500 focus:outline-none";
export default async function Join({params,searchParams}:{params:{slug:string},searchParams:{done?:string}}){await db();const tenant=await Tenant.findOne({enrollmentSlug:params.slug}).lean();if(!tenant)notFound();if(searchParams.done)return <main className="mx-auto flex min-h-screen max-w-md flex-col items-center justify-center p-6 text-center"><CheckCircle2 className="mb-4 h-16 w-16 text-emerald-500"/><h1 className="text-2xl font-bold text-slate-900">Request submitted!</h1><p className="mt-2 text-sm text-slate-500">{tenant.tuitionName} will confirm your admission shortly.</p></main>;return <main className="mx-auto min-h-screen max-w-md bg-slate-50 p-4 py-10"><div className="mb-6 text-center"><div className="mx-auto mb-4 flex h-16 w-16 items-center justify-center rounded-2xl bg-gradient-to-tr from-orange-500 to-amber-500 text-white shadow-lg shadow-orange-500/20">{tenant.logoUrl?<img src={tenant.logoUrl} className="h-full w-full rounded-2xl object-cover" alt=""/>:<UserPlus className="h-8 w-8"/>}</div><h1 className="text-xl font-bold text-slate-900">{tenant.tuitionName}</h1><p className="mt-1 text-sm text-slate-500">Student enrollment request</p></div><form className="space-y-4 rounded-3xl border border-slate-100 bg-white p-6 shadow-sm" action={`/api/enrollment/${params.slug}`} method="post"><label className="block space-y-1 text-xs font-semibold uppercase text-slate-500">Student&apos;s Name<input className={input} name="name" required maxLength={80}/></label><label className="block space-y-1 text-xs font-semibold uppercase text-slate-500">Parent&apos;s WhatsApp Number<input className={input} name="parentPhone" required inputMode="numeric" pattern="[0-9]{10}"/></label><label className="block space-y-1 text-xs font-semibold uppercase text-slate-500">Class / Batch<input className={input} name="class" required maxLength={50}/></label><button className="w-full rounded-2xl bg-gradient-to-r from-orange-500 to-amber-500 py-3.5 text-sm font-bold text-white shadow-md shadow-orange-500/20">Submit Request</button></form></main>}
