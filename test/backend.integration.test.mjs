import test from"node:test";import assert from"node:assert/strict";import mongoose from"mongoose";import{tenantFilter}from"../lib/tenant.mjs";
const uri=process.env.TEST_MONGODB_URI||"mongodb://127.0.0.1:27017/teacher-tool-test";
test("tenant filters cannot be overridden and isolate records",async()=>{await mongoose.connect(uri);const Record=mongoose.models.IsolationRecord||mongoose.model("IsolationRecord",new mongoose.Schema({tenantId:String,name:String}));await Record.deleteMany({});await Record.create([{tenantId:"tenant-a",name:"A"},{tenantId:"tenant-b",name:"B"}]);const filter=tenantFilter("tenant-a",{tenantId:"tenant-b"});assert.equal(filter.tenantId,"tenant-a");assert.deepEqual((await Record.find(filter).lean()).map(x=>x.name),["A"]);await Record.deleteMany({});await mongoose.disconnect()});
